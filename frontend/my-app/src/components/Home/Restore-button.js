import React from "react";

const RestoreButton = () => {
	return(
		<a id="restore-button" className="h5 sub-title-button" href="/restore-database">Restore teams database</a>
	);
};

export default RestoreButton;